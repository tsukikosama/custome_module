# 员工内部系统开发文档

## 项目概述
员工内部积分商城系统，支持员工通过加班等方式获得积分，使用积分兑换商品，管理员进行订单管理和采购处理。

---

# 模块一：订单管理

## 1. 接口概述
订单管理模块包含用户下单、订单查询、管理员订单处理等功能，支持完整的订单生命周期管理。

## 2. 数据模型

### 2.1 订单状态枚举
```java
public enum OrderStatus {
    PENDING(1, "待处理"),           // 初始状态，等待管理员处理
    PURCHASED(2, "已采购"),         // 已采购
    COMPLETED(3, "已完成"),          // 订单完成
    CANCELLED(4, "已取消"),          // 订单取消
    DELIVERED(5, "已交付");          // 订单已交付
}
```

### 2.2 主要数据表
- `biz_order`：订单主表
- `biz_product`：商品表
- `biz_points_log`：积分日志表

### 2.3 订单表结构 (biz_order)
```sql
CREATE TABLE `biz_order` (
  `id` bigint NOT NULL,
  `order_no` varchar(64) NOT NULL COMMENT '订单号',
  `product_id` bigint NOT NULL COMMENT '商品ID',
  `product_num` int NOT NULL COMMENT '商品数量',
  `status` tinyint NOT NULL COMMENT '订单状态',
  `cost_points` decimal(10,2) NOT NULL COMMENT '花费的积分',
  `create_time` datetime NOT NULL COMMENT '创建时间',
  `create_user` bigint NOT NULL COMMENT '创建人',
  `update_time` datetime DEFAULT NULL COMMENT '更新时间',
  `update_user` bigint DEFAULT NULL COMMENT '更新人',
  `deleted` bigint DEFAULT '0' COMMENT '是否已删除（0：否；id：是）',
  `cancel_time` datetime DEFAULT NULL COMMENT '取消时间',
  `finish_time` datetime DEFAULT NULL COMMENT '完成时间',
  `remark` varchar(255) DEFAULT NULL COMMENT '备注',
  PRIMARY KEY (`id`),
  KEY `index_status` (`status`)
) ENGINE=InnoDB DEFAULT COMMENT='订单表'
```

---

## 3. 客户端接口

### 3.1 下单接口
**功能描述**：用户使用积分兑换商品，扣除对应积分，创建订单

**业务规则**：
- 校验商品是否存在且已上架 (is_shelf = 1)
- 校验用户积分是否足够
- 自动计算消耗积分 = 商品积分 × 商品数量
- 扣除用户积分并记录积分日志（type为积分来源，amount为负数表示扣除）
- 生成订单号（格式：ORD + yyyyMMddHHmmss + 4位随机数）
- 订单初始状态为"待处理"(1)

**请求信息**：
```
POST /api/order
Content-Type: application/json
Authorization: Bearer {token}
```

**请求参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| productId | Long | 是 | 商品ID | 1 |
| productNum | Integer | 是 | 商品数量 | 2 |

**请求示例**：
```json
{
  "productId": 1,
  "productNum": 2
}
```

**成功响应**：
```json
{
  "code": 0,
  "msg": "下单成功",
  "data": {
    "orderId": 123,
    "orderNo": "ORD20260429103001234"
  }
}
```

**异常响应**：
```json
// 积分不足
{
  "code": 4001,
  "msg": "积分不足，当前积分：100，需要：200"
}

// 商品不存在
{
  "code": 4002,
  "msg": "商品不存在或已下架"
}
```

**涉及表**：
- INSERT: biz_order
- UPDATE: sys_user (扣除积分)
- INSERT: biz_points_log (记录积分变动)

---

### 3.2 取消订单接口
**功能描述**：用户取消订单，退还积分

**业务规则**：
- 只能取消状态为"待处理"(1)的订单
- 取消后退还用户积分
- 记录积分日志（type为积分来源，amount为正数表示增加）
- 订单状态变更为"已取消"(4)

**请求信息**：
```
PUT /api/order/cancel/{orderId}
Content-Type: application/json
Authorization: Bearer {token}
```

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| orderId | Long | 是 | 订单ID | 123 |

**请求参数**（Body）：
```json
{}  // 空对象，无需参数
```

**成功响应**：

```json
{
  "code": 0,
  "msg": "订单已取消，积分已退还"
}
```

**异常响应**：
```json
// 订单状态不允许取消
{
  "code": 4003,
  "msg": "该订单状态不允许取消"
}

// 订单不存在
{
  "code": 4004,
  "msg": "订单不存在"
}
```

---

### 3.3 订单详情接口
**功能描述**：查询单个订单的详细信息

**业务规则**：
- 只能查询自己的订单
- 包含商品基本信息和订单状态
- 关联查询商品名称、商品类型名称

**请求信息**：
```
GET /api/order/{orderId}
Authorization: Bearer {token}
```

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| orderId | Long | 是 | 订单ID | 123 |

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "orderId": 123,
    "orderNo": "ORD20260429103001234",
    "productId": 1,
    "productName": "机械键盘",
    "productType": "电子产品",
    "productNum": 2,
    "costPoints": 500,
    "status": 1,
    "statusText": "待处理",
    "remark": null,
    "createTime": "2026-04-29 10:30:00"
  }
}
```

---

### 3.4 订单分页查询接口
**功能描述**：分页查询当前用户的订单列表

**业务规则**：
- 只查询当前用户的订单
- 支持按订单状态筛选
- 支持排序（创建时间升序/降序、积分升序/降序）
- 默认按创建时间倒序排列

**请求信息**：
```
GET /api/order
Authorization: Bearer {token}
```

**查询参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| current | Integer | 是 | 当前页码，从1开始 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| status | Integer | 否 | 订单状态筛选（1待处理/2已采购/3已完成/4已取消） | 1 |
| sortField | String | 否 | 排序字段（createTime创建时间/costPoints花费积分） | createTime |
| sortOrder | String | 否 | 排序方式（asc升序/desc降序） | desc |

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "records": [
      {
        "orderId": 123,
        "orderNo": "ORD20260429103001234",
        "productName": "机械键盘",
        "productNum": 2,
        "costPoints": 500,
        "status": 1,
        "statusText": "待处理",
        "createTime": "2026-04-29 10:30:00"
      }
    ],
    "total": 100,
    "page": 1,
    "size": 10
  }
}
```

---

## 4. 后台管理接口

### 4.1 订单列表查询接口（CRUD查询）
**功能描述**：后台管理员分页查询所有订单

**业务规则**：
- 支持按订单号、用户ID、状态等多条件筛选
- 支持按时间范围查询
- 默认按创建时间倒序
- 关联查询用户信息、商品信息

**请求信息**：
```
GET /biz/order/page
Authorization: Bearer {adminToken}
```

**查询参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| current | Integer | 是 | 当前页码 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| orderNo | String | 否 | 订单号（模糊查询） | ORD20260429 |
| userId | Long | 否 | 用户ID | 10 |
| status | Integer | 否 | 订单状态 | 1 |
| startTime | String | 否 | 开始时间 | 2026-04-01 |
| endTime | String | 否 | 结束时间 | 2026-04-30 |

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "records": [
      {
        "orderId": 123,
        "orderNo": "ORD20260429103001234",
        "userId": 10,
        "username": "张三",
        "productName": "机械键盘",
        "productNum": 2,
        "costPoints": 500,
        "status": 1,
        "statusText": "待处理",
        "createTime": "2026-04-29 10:30:00"
      }
    ],
    "total": 500,
    "page": 1,
    "size": 10
  }
}
```

---

### 4.2 取消订单接口（管理员强制取消）
**功能描述**：管理员强制取消订单

**业务规则**：
- 只能取消状态为"待处理"(1)的订单
- 必须填写取消原因
- 退还用户积分
- 记录操作人和操作时间

**请求信息**：
```
PUT /biz/order/refund/{orderId}
Content-Type: application/json
```

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| orderId | Long | 是 | 订单ID | 123 |

**请求参数**：
```json
{
  "remark": "库存不足，无法采购"
}
```

**成功响应**：
```json
{
  "code": 0,
  "msg": "订单已取消"
}
```

**异常响应**：
```json
// 订单不存在
{
  "code": 4004,
  "msg": "订单不存在"
}

// 取消原因不能为空
{
  "code": 4005,
  "msg": "取消原因不能为空"
}
```

---

### 4.3 完成采购接口
**功能描述**：管理员确认采购完成，订单进入已采购状态

**业务规则**：
- 只能操作"待处理"(1)状态的订单
- 状态变更为"已采购"(2)
- 记录操作人和操作时间

**请求信息**：
```
PUT /biz/order/procurement/{orderId}
Content-Type: application/json
```

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| orderId | Long | 是 | 订单ID | 123 |

**请求参数**：
```json
{}  // 空对象，无需参数
```

**成功响应**：
```json
{
  "code": 0,
  "msg": "采购完成，订单已采购"
}
```

**异常响应**：
```json
// 订单状态不允许
{
  "code": 4003,
  "msg": "该订单状态不允许此操作"
}
```

---

### 4.4 完成订单接口
**功能描述**：管理员确认订单完成

**业务规则**：
- 只能操作"已采购"(2)状态的订单
- 状态变更为"已完成"(3)
- 记录操作人和操作时间

**请求信息**：
```
PUT /biz/order/finish/{orderId}
Content-Type: application/json
```

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| orderId | Long | 是 | 订单ID | 123 |

**请求参数**：
```json
{}  // 空对象，无需参数
```

**成功响应**：
```json
{
  "code": 0,
  "msg": "订单已完成"
}
```

**异常响应**：
```json
// 订单状态不允许
{
  "code": 4003,
  "msg": "该订单状态不允许此操作"
}
```

---

### 4.5 订单详情接口（后台）
**功能描述**：后台查询单个订单的详细信息

**业务规则**：
- 查询任意订单的完整信息
- 包含用户信息、商品信息

**请求信息**：
```
GET /biz/order/{orderId}
```

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| orderId | Long | 是 | 订单ID | 123 |

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "orderId": 123,
    "orderNo": "ORD20260429103001234",
    "productId": 1,
    "productName": "机械键盘",
    "productType": "电子产品",
    "productNum": 2,
    "costPoints": 500,
    "status": 1,
    "statusText": "待处理",
    "userId": 10,
    "username": "张三",
    "userPoints": 500,
    "createTime": "2026-04-29 10:30:00",
    "updateTime": null
  }
}
```

---

## 5. 公共响应结构

所有接口统一使用以下响应结构：
```json
{
  "code": 0,           // 状态码：0成功，其他失败
  "msg": "操作成功",      // 提示信息
  "data": {}             // 返回数据，可为null或对象/数组
}
```

## 6. 权限要求
| 接口路径 | 需要登录 | 需要权限 |
|---------|---------|---------|
| /api/order/* | 是 | 普通用户权限 |
| /api/product | 是 | 普通用户权限 |
| /biz/order/* | 是 | 管理员权限 |
| /biz/product/* | 是 | 管理员权限 |

## 7. 数据字典

### 7.1 订单状态 (biz_order.status)
| 值 | 说明 | 可执行操作 |
|----|------|-----------|
| 1 | 待处理 | 用户取消、管理员开始采购 |
| 2 | 已采购 | 管理员完成采购并交付给客户、管理员直接完成订单（虚拟商品等） |
| 3 | 已完成 | 无（终态） |
| 4 | 已取消 | 无（终态） |
| 5 | 已交付 | 用户确认完成 |

### 7.2 订单流转流程

订单状态流转规则如下：

#### 正常流程
```
标准流程（实物商品）：
用户下单 → 待处理(1) → 已采购(2) → 已交付(5) → 已完成(3)
           ↓            ↓
        用户取消      管理员完成采购并交付

快速流程（虚拟商品）：
用户下单 → 待处理(1) → 已采购(2) → 已完成(3)
           ↓            ↓
        用户取消      管理员直接完成
```

#### 详细说明

**1. 用户下单**
- 用户创建订单
- 订单初始状态为"待处理"(1)
- 扣除用户积分

**2. 管理员开始采购**
- 管理员查看"待处理"(1)状态的订单
- 将订单状态更新为"已采购"(2)

**3. 管理员完成采购并交付**
- 管理员采购完成
- 将商品交给客户
- 将订单状态更新为"已交付"(5)

**4. 用户确认完成**
- 用户收到商品后确认
- 将订单状态更新为"已完成"(3)

**5. 自动完成机制**
- 订单状态为"已交付"(5)后
- 如果7天内用户未确认
- 系统自动将状态更新为"已完成"(3)

**6. 管理员直接完成订单（虚拟商品场景）**
- 对于虚拟商品（如电子券、会员卡等）或无需物流配送的商品
- 管理员在"已采购"(2)状态时可直接完成订单
- 将订单状态从"已采购"(2)直接更新为"已完成"(3)
- 无需经过"已交付"(5)状态

#### 取消流程
- 只有"待处理"(1)状态的订单可以取消
- 用户可以主动取消自己的订单
- 管理员也可以强制取消订单
- 取消后状态变为"已取消"(4)
- 取消后退还用户积分

#### 状态流转表
| 当前状态 | 可流转状态 | 操作人 | 说明 |
|---------|-----------|--------|------|
| 待处理(1) | 已采购(2) | 管理员 | 管理员开始采购 |
| 待处理(1) | 已取消(4) | 用户/管理员 | 取消订单，退还积分 |
| 已采购(2) | 已交付(5) | 管理员 | 采购完成，已交付给客户（实物商品） |
| 已采购(2) | 已完成(3) | 管理员 | 管理员直接完成订单（虚拟商品等） |
| 已交付(5) | 已完成(3) | 用户 | 用户确认收货 |
| 已交付(5) | 已完成(3) | 系统 | 7天未确认自动完成 |
| 已完成(3) | - | - | 终态，不可再变更 |
| 已取消(4) | - | - | 终态，不可再变更 |

### 7.3 积分日志记录规则 (biz_points_log)
- **type**：积分来源类型（数字类型，如加班、订单退还等）
- **amount**：积分变动数量（正数表示增加，负数表示扣除）

---

# 模块二：商品管理

## 1. 接口概述
商品管理模块包含商品的增删改查和上下架功能，管理员通过后台管理商品信息。

## 2. 数据模型

### 2.1 商品表结构 (biz_product)
```sql
CREATE TABLE `biz_product` (
  `id` bigint NOT NULL,
  `name` varchar(100) NOT NULL COMMENT '商品名称',
  `type` varchar(50) DEFAULT NULL COMMENT '商品类型',
  `points` decimal(10,2) NOT NULL COMMENT '所需积分',
  `monthly_limit` int DEFAULT NULL COMMENT '每月限兑次数',
  `is_shelf` tinyint NOT NULL DEFAULT '1' COMMENT '是否上架（0：下架；1：上架）',
  `image` varchar(255) DEFAULT NULL COMMENT '商品图片',
  `description` varchar(500) DEFAULT NULL COMMENT '商品描述',
  `create_time` datetime NOT NULL COMMENT '创建时间',
  `create_user` bigint NOT NULL COMMENT '创建人',
  `update_time` datetime DEFAULT NULL COMMENT '更新时间',
  `update_user` bigint DEFAULT NULL COMMENT '更新人',
  `deleted` bigint DEFAULT '0' COMMENT '是否已删除（0：否；id：是）',
  PRIMARY KEY (`id`),
  KEY `index_is_shelf` (`is_shelf`)
) ENGINE=InnoDB DEFAULT COMMENT='商品表'
```

### 2.2 商品上架状态
| 值 | 说明 |
|----|------|
| 0 | 下架 |
| 1 | 上架 |

---

## 3. 客户端接口

### 3.1 商品分页查询接口
**功能描述**：分页查询已上架的商品列表

**业务规则**：
- 只查询已上架的商品 (is_shelf = 1)
- 支持按商品名称模糊查询
- 支持按商品类型筛选
- 支持排序（积分升序/降序、时间升序/降序）

**请求信息**：
```
GET /api/product
Authorization: Bearer {token}
```

**查询参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| current | Integer | 是 | 当前页码，从1开始 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| name | String | 否 | 商品名称（模糊查询） | 键盘 |
| type | String | 否 | 商品类型 | 电子产品 |
| sortField | String | 否 | 排序字段（points积分/time创建时间） | points |
| sortOrder | String | 否 | 排序方式（asc升序/desc降序） | desc |

**请求示例**：
```
GET /api/product?current=1&size=10&name=键盘&type=电子产品&sortField=points&sortOrder=desc
```

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "records": [
      {
        "id": 1,
        "name": "机械键盘",
        "points": 500.00,
        "image": "https://example.com/images/keyboard.jpg",
        "monthlyLimit": 2,
        "monthlyPurchasedCount": 1
      },
      {
        "id": 2,
        "name": "无线鼠标",
        "points": 300.00,
        "image": "https://example.com/images/mouse.jpg",
        "monthlyLimit": 5,
        "monthlyPurchasedCount": 0
      }
    ],
    "total": 50,
    "page": 1,
    "size": 10
  }
}
```

**响应字段说明**：
| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | Long | 商品ID |
| name | String | 商品名称 |
| points | Decimal | 商品所需积分 |
| image | String | 商品图片URL |
| monthlyLimit | Integer | 每月限兑次数，null表示不限次数 |
| monthlyPurchasedCount | Integer | 当前用户本月已购买该商品的次数（自然月） |

---

## 4. 后台管理接口

### 4.1 商品CRUD接口
**功能描述**：商品的增删改查基础接口（框架自动生成）

**请求信息**：
```
POST   /biz/product         # 新增商品
PUT    /biz/product         # 修改商品
DELETE /biz/product/{id}    # 删除商品
GET    /biz/product/{id}    # 查询商品详情
GET    /biz/product/page    # 分页查询商品列表
```

**权限要求**：需要管理员权限

---

### 4.2 商品上下架接口
**功能描述**：设置商品的上架/下架状态

**请求信息**：
```
PUT /biz/product/{productId}/shelf
Authorization: Bearer {adminToken}
```

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| productId | Long | 是 | 商品ID | 1 |

**请求参数**：
```json
{
  "isShelf": 0
}
```

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| isShelf | Integer | 是 | 上架状态（0下架/1上架） | 0 |

**请求示例**：
```
PUT /biz/product/1/shelf
Content-Type: application/json

{
  "isShelf": 0
}
```

**成功响应**：
```json
{
  "code": 0,
  "msg": "操作成功"
}
```

**异常响应**：
```json
// 商品不存在
{
  "code": 4007,
  "msg": "商品不存在"
}
```

**业务规则**：
- 下架商品后，用户无法在下单时选择该商品
- 上架状态变更后立即生效

---

# 模块三：积分管理

## 1. 接口概述
积分管理模块包含积分日志查询功能，用户可以查看自己的积分变动记录。

## 2. 数据模型

### 2.1 积分日志表结构 (biz_points_log)
```sql
CREATE TABLE `biz_points_log` (
  `id` bigint NOT NULL,
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `type` tinyint NOT NULL COMMENT '积分类型（1：加班转换；2：兑换商品；3：积分失效；4：积分退还；5：违规清除；6：积分修正；7：活动获得）',
  `amount` decimal(10,2) NOT NULL COMMENT '积分变动数量（正数表示增加，负数表示扣除）',
  `remark` varchar(255) DEFAULT NULL COMMENT '备注说明',
  `create_time` datetime NOT NULL COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `index_user_id` (`user_id`),
  KEY `index_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT COMMENT='积分日志表'
```

### 2.2 积分类型字典 (PointsTypeEnum)
| 值 | 说明 | 积分变化 |
|----|------|---------|
| 1 | 加班转换 | 正数（增加） |
| 2 | 兑换商品 | 负数（扣除） |
| 3 | 积分失效 | 负数（扣除） |
| 4 | 积分退还 | 正数（增加） |
| 5 | 违规清除 | 负数（扣除） |
| 6 | 积分修正 | 正数或负数 |
| 7 | 活动获得 | 正数（增加） |

---

## 3. 客户端接口

### 3.1 积分日志分页查询接口
**功能描述**：分页查询当前用户的积分变动记录

**业务规则**：
- 只查询当前登录用户的积分日志
- 支持按时间范围筛选
- 支持按积分类型筛选
- 支持排序（创建时间升序/降序、积分数量升序/降序）
- 默认按创建时间倒序排列（最新的在前面）

**请求信息**：

```
GET /api/pointLog
Authorization: Bearer {token}
```

**查询参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| page | Integer | 是 | 当前页码，从1开始 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| times[] | Array<String> | 否 | 时间范围数组，第一个是开始时间，第二个是结束时间（格式：yyyy-MM-dd HH:mm:ss） | ["2026-04-01 00:00:00", "2026-04-30 23:59:59"] |
| type | Integer | 否 | 积分类型（1加班转换/2兑换商品/3积分失效/4积分退还/5违规清除/6积分修正/7活动获得） | 1 |
| sortField | String | 否 | 排序字段（createTime创建时间/amount积分数量） | createTime |
| sortOrder | String | 否 | 排序方式（asc升序/desc降序） | desc |

**请求示例**：
```
GET /api/pointLog?page=1&size=10&times[]=2026-04-01 00:00:00&times[]=2026-04-30 23:59:59&type=1
```

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "records": [
      {
        "id": 1,
        "time": "2026-04-29 10:30:00",
        "type": 1,
        "typeText": "加班转换",
        "amount": 100.00,
        "remark": "2026年4月加班积分"
      },
      {
        "id": 2,
        "time": "2026-04-28 15:20:00",
        "type": 4,
        "typeText": "积分退还",
        "amount": 500.00,
        "remark": "取消订单：ORD20260428152000001"
      },
      {
        "id": 3,
        "time": "2026-04-27 09:00:00",
        "type": 7,
        "typeText": "活动获得",
        "amount": 10.00,
        "remark": "参与活动奖励"
      }
    ],
    "total": 50,
    "page": 1,
    "size": 10
  }
}
```

**响应字段说明**：
| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | Long | 日志ID |
| time | String | 积分变动时间（格式：yyyy-MM-dd HH:mm:ss） |
| type | Integer | 积分类型 |
| typeText | String | 积分类型文本描述 |
| amount | Decimal | 积分变动数量（正数表示增加，负数表示扣除） |
| remark | String | 备注说明 |

---

## 4. 后台管理接口

### 4.1 积分日志CRUD接口



**功能描述**：积分日志的增删改查基础接口（框架自动生成）

**请求信息**：
```
POST   /biz/pointsLog         # 新增积分日志
PUT    /biz/pointsLog         # 修改积分日志
DELETE /biz/pointsLog/{id}    # 删除积分日志
GET    /biz/pointsLog/{id}    # 查询积分日志详情
GET    /biz/pointsLog/page    # 分页查询积分日志列表（支持筛选用户）
```

**权限要求**：需要管理员权限

**查询参数（分页查询）**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| current | Integer | 是 | 当前页码 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| userId | Long | 否 | 用户ID | 10 |
| type | Integer | 否 | 积分类型 | 1 |
| times[] | Array<String> | 否 | 时间范围数组，第一个是开始时间，第二个是结束时间（格式：yyyy-MM-dd HH:mm:ss） | ["2026-04-01", "2026-04-30"] |

---

### 4.2 积分导入接口
**功能描述**：管理员通过Excel文件导入用户积分

**业务规则**：
- 管理员上传Excel文件更新用户积分
- 积分来源ID（refId）只能选择一个，前端必须做单选校验
- 积分类型作为全局参数，应用于整个批次
- 根据用户名称匹配用户
- 自动插入积分日志记录并更新用户积分
- 如果部分用户导入失败，返回失败用户列表，已成功的用户不受影响

**请求信息**：
```
POST /system/user/points/import
Content-Type: multipart/form-data
Authorization: Bearer {adminToken}
```

**请求参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| refId | Long | 是 | 积分来源ID | 100 |
| type | Integer | 是 | 积分类型（1加班转换/2兑换商品/3积分失效/4积分退还/5违规清除/6积分修正/7活动获得） | 6 |
| remark | String | 否 | 备注说明 | 年终积分调整 |
| file | File | 是 | Excel文件（.xlsx或.xls格式） | points_import.xlsx |

**Excel文件格式**：
Excel文件需包含以下列（第一行为表头）：

| 列名 | 类型 | 必填 | 说明 | 示例 |
|-----|------|------|------|------|
| 用户名称 | String | 是 | 用户昵称或真实姓名 | 张三 |
| 积分数量 | Decimal | 是 | 积分变动数量（正数表示增加，负数表示扣除） | 100.00 |

**Excel示例**：
```
用户名称    积分数量
张三        100.00
李四        -50.00
王五        200.00
```

**请求示例**：
```
POST /system/user/points/import
Content-Type: multipart/form-data

refId=100
type=6
remark=年终积分调整
file=@points_import.xlsx
```

**成功响应**：
```json
{
  "code": 0,
  "msg": "积分导入完成",
  "data": {
    "totalCount": 10,
    "successCount": 8,
    "failCount": 2,
    "failedUsers": [
      {
        "userName": "赵六",
        "reason": "用户不存在"
      }
    ]
  }
}
```

**异常响应**：
```json
// 文件格式错误
{
  "code": 4027,
  "msg": "文件格式错误，请上传Excel文件"
}

// 文件为空
{
  "code": 4028,
  "msg": "文件内容为空"
}

// 积分类型参数错误
{
  "code": 400,
  "msg": "积分类型参数错误"
}

// 未登录或权限不足
{
  "code": 401,
  "msg": "请先登录或权限不足"
}
```

**响应字段说明**：
| 字段名 | 类型 | 说明 |
|-------|------|------|
| totalCount | Integer | 总记录数（Excel中的有效行数） |
| successCount | Integer | 成功导入数量 |
| failCount | Integer | 失败数量 |
| failedUsers | Array<Object> | 失败用户列表 |
| failedUsers.userName | String | 失败的用户名称 |
| failedUsers.reason | String | 失败原因 |

**业务规则细节**：
- 用户名称匹配：优先匹配昵称，若昵称不存在则匹配真实姓名
- 用户不唯一时：返回"用户名称不唯一"错误，需要管理员使用更精确的名称
- 积分数量校验：必须为有效的数字格式
- 事务处理：每个用户独立处理，部分失败不影响已成功的用户
- 积分日志：每条成功记录都会插入一条积分日志，type为请求参数指定的类型，refId为请求参数指定的积分来源ID
- 备注说明：remark参数可选，如果提供则作为所有积分记录的统一备注

**涉及表**：
- INSERT: biz_points_log（批量插入积分日志）
- UPDATE: sys_user（批量更新用户积分）

---

# 模块四：加班记录管理

## 1. 接口概述
加班记录模块包含员工加班申请和记录管理功能，管理员审核加班记录后自动为员工增加积分。

## 2. 数据模型

### 2.1 加班记录表结构 (biz_overtime)
```sql
CREATE TABLE `biz_overtime` (
  `id` bigint NOT NULL,
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `overtime_date` date NOT NULL COMMENT '加班日期',
  `start_time` time NOT NULL COMMENT '开始时间',
  `end_time` time NOT NULL COMMENT '结束时间',
  `duration` decimal(4,2) NOT NULL COMMENT '加班时长（小时）',
  `points` decimal(10,2) NOT NULL COMMENT '获得积分',
  `status` tinyint NOT NULL DEFAULT '1' COMMENT '状态（1：待审核；2：已通过；3：已拒绝）',
  `result` tinyint DEFAULT NULL COMMENT '结果（1：有效；2：无效）',
  `remark` varchar(255) DEFAULT NULL COMMENT '备注说明',
  `audit_remark` varchar(255) DEFAULT NULL COMMENT '审核备注',
  `audit_time` datetime DEFAULT NULL COMMENT '审核时间',
  `audit_user` bigint DEFAULT NULL COMMENT '审核人ID',
  `create_time` datetime NOT NULL COMMENT '创建时间',
  `create_user` bigint NOT NULL COMMENT '创建人',
  `update_time` datetime DEFAULT NULL COMMENT '更新时间',
  `update_user` bigint DEFAULT NULL COMMENT '更新人',
  `deleted` bigint DEFAULT '0' COMMENT '是否已删除（0：否；id：是）',
  PRIMARY KEY (`id`),
  KEY `index_user_id` (`user_id`),
  KEY `index_status` (`status`),
  KEY `index_overtime_date` (`overtime_date`)
) ENGINE=InnoDB DEFAULT COMMENT='加班记录表'
```

### 2.2 加班状态字典 (status)
| 值 | 说明 | 可执行操作 |
|----|------|-----------|
| 1 | 待审核 | 管理员审核 |
| 2 | 已通过 | 无 |
| 3 | 已拒绝 | 无 |

### 2.3 加班结果字典 (result)
| 值 | 说明 |
|----|------|
| 1 | 有效（已获得积分） |
| 2 | 无效（未获得积分） |

---

## 3. 客户端接口

### 3.1 加班记录分页查询接口
**功能描述**：分页查询当前用户的加班记录

**业务规则**：
- 只查询当前登录用户的加班记录
- 支持按状态筛选
- 支持按结果筛选
- 支持按创建时间范围筛选
- 支持排序（加班日期升序/降序、创建时间升序/降序、积分升序/降序）
- 默认按加班日期倒序排列（最新的在前面）

**请求信息**：
```
GET /api/overtime
Authorization: Bearer {token}
```

**查询参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| page | Integer | 是 | 当前页码，从1开始 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| status | Integer | 否 | 状态筛选（1待审核/2已通过/3已拒绝） | 2 |
| result | Integer | 否 | 结果筛选（1有效/2无效） | 1 |
| times[] | Array<String> | 否 | 创建时间范围数组（格式：yyyy-MM-dd HH:mm:ss） | ["2026-04-01 00:00:00", "2026-04-30 23:59:59"] |
| sortField | String | 否 | 排序字段（overtimeDate加班日期/createTime创建时间/points积分） | overtimeDate |
| sortOrder | String | 否 | 排序方式（asc升序/desc降序） | desc |

**请求示例**：
```
GET /api/overtime/page?page=1&size=10&status=2&result=1&times[]=2026-04-01 00:00:00&times[]=2026-04-30 23:59:59
```

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "records": [
      {
        "id": 1,
        "overtimeDate": "2026-04-29",
        "startTime": "18:00:00",
        "endTime": "22:00:00",
        "duration": 4.00,
        "points": 40.00,
        "status": 2,
        "statusText": "已通过",
        "result": 1,
        "resultText": "有效",
        "remark": "项目紧急加班",
        "createTime": "2026-04-29 10:00:00"
      },
      {
        "id": 2,
        "overtimeDate": "2026-04-28",
        "startTime": "18:30:00",
        "endTime": "21:30:00",
        "duration": 3.00,
        "points": 30.00,
        "status": 2,
        "statusText": "已通过",
        "result": 1,
        "resultText": "有效",
        "remark": "系统维护",
        "createTime": "2026-04-28 09:00:00"
      },
      {
        "id": 3,
        "overtimeDate": "2026-04-27",
        "startTime": "19:00:00",
        "endTime": "23:00:00",
        "duration": 4.00,
        "points": 0.00,
        "status": 3,
        "statusText": "已拒绝",
        "result": 2,
        "resultText": "无效",
        "remark": "未提前申请",
        "auditRemark": "未提前申请，不予通过",
        "createTime": "2026-04-27 08:00:00"
      }
    ],
    "total": 30,
    "page": 1,
    "size": 10
  }
}
```

**响应字段说明**：
| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | Long | 加班记录ID |
| overtimeDate | String | 加班日期（格式：yyyy-MM-dd） |
| startTime | String | 开始时间（格式：HH:mm:ss） |
| endTime | String | 结束时间（格式：HH:mm:ss） |
| duration | Decimal | 加班时长（小时） |
| points | Decimal | 获得的积分 |
| status | Integer | 状态 |
| statusText | String | 状态文本描述 |
| result | Integer | 结果 |
| resultText | String | 结果文本描述 |
| remark | String | 备注说明 |
| auditRemark | String | 审核备注 |
| createTime | String | 创建时间（格式：yyyy-MM-dd HH:mm:ss） |

---

## 4. 后台管理接口

### 4.1 加班记录CRUD接口
**功能描述**：加班记录的增删改查基础接口（框架自动生成）

**请求信息**：
```
POST   /biz/overtime         # 新增加班记录
PUT    /biz/overtime         # 修改加班记录
DELETE /biz/overtime/{id}    # 删除加班记录
GET    /biz/overtime/{id}    # 查询加班记录详情
GET    /biz/overtime/page    # 分页查询加班记录列表（支持筛选用户）
```

**权限要求**：需要管理员权限

**查询参数（分页查询）**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| current | Integer | 是 | 当前页码 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| userId | Long | 否 | 用户ID | 10 |
| status | Integer | 否 | 状态 | 2 |
| result | Integer | 否 | 结果 | 1 |
| times[] | Array<String> | 否 | 创建时间范围数组 | ["2026-04-01", "2026-04-30"] |

### 4.2 审核加班记录接口
**功能描述**：管理员审核加班记录，通过后自动为用户增加积分

**请求信息**：
```
PUT /biz/overtime/audit/{overtimeId}
Content-Type: application/json
Authorization: Bearer {adminToken}
```

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| overtimeId | Long | 是 | 加班记录ID | 1 |

**请求参数**：
```json
{
  "status": 2,
  "result": 1,
  "auditRemark": "审核通过"
}
```

| 参数名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| status | Integer | 是 | 审核状态（2：已通过；3：已拒绝） |
| result | Integer | 否 | 审核结果（1：有效；2：无效），审核通过时必填 |
| auditRemark | String | 否 | 审核备注 |

**成功响应**：
```json
{
  "code": 0,
  "msg": "审核成功"
}
```

**异常响应**：
```json
// 加班记录不存在
{
  "code": 4008,
  "msg": "加班记录不存在"
}

// 记录已审核
{
  "code": 4009,
  "msg": "该记录已审核，不能重复操作"
}
```

**业务规则**：
- 只能审核状态为"待审核"(1)的记录
- 审核通过且结果为"有效"时，自动为用户增加积分
- 审核通过后记录审核人和审核时间
- 审核拒绝时，result可以为null或2（无效）

---

# 模块五：建议管理

## 1. 接口概述
建议管理模块包含员工提交建议/反馈的功能，员工可以提交建议给管理员，管理员通过后台查看和处理。

## 2. 数据模型

### 2.1 建议表结构 (biz_suggestion)
```sql
CREATE TABLE `biz_suggestion` (
  `id` bigint NOT NULL COMMENT 'id',
  `tag` varchar(10) NOT NULL COMMENT '标签',
  `content` text NOT NULL COMMENT '内容',
  `create_time` datetime NOT NULL COMMENT '创建时间',
  `create_user` bigint NOT NULL COMMENT '创建人',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB  COMMENT='建议表'
```

### 2.2 建议标签字典 (tag)
| 值 | 说明 |
|----|------|
| 1 | 功能建议 |
| 2 | Bug反馈 |
| 3 | 体验优化 |
| 4 | 其他 |

---

## 3. 客户端接口

### 3.1 建议列表分页查询接口
**功能描述**：分页查询当前登录用户的建议列表

**业务规则**：
- 用户必须登录才能使用
- 只查询当前登录用户的建议
- 支持排序（创建时间升序/降序、标签升序/降序）
- 默认按创建时间倒序排列（最新的在前面）

**请求信息**：
```
GET /api/suggestion
Authorization: Bearer {token}
```

**查询参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| page | Integer | 是 | 当前页码，从1开始 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| sortField | String | 否 | 排序字段（createTime创建时间/tag标签） | createTime |
| sortOrder | String | 否 | 排序方式（asc升序/desc降序） | desc |

**请求示例**：
```
GET /api/suggestion?page=1&size=10
```

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "records": [
      {
        "id": 1,
        "createTime": "2026-04-29 10:30:00",
        "tag": 1,
        "tagText": "功能建议",
        "content": "希望增加一个商品收藏功能，方便收藏感兴趣的商品"
      },
      {
        "id": 2,
        "createTime": "2026-04-28 15:20:00",
        "tag": 2,
        "tagText": "Bug反馈",
        "content": "商品详情页在手机端显示异常，图片无法加载"
      },
      {
        "id": 3,
        "createTime": "2026-04-27 09:00:00",
        "tag": 3,
        "tagText": "体验优化",
        "content": "建议增加夜间模式，保护视力"
      }
    ],
    "total": 20,
    "page": 1,
    "size": 10
  }
}
```

**响应字段说明**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | Long | 建议ID |
| createTime | String | 创建时间（格式：yyyy-MM-dd HH:mm:ss） |
| tag | Integer | 标签编码 |
| tagText | String | 标签文本描述 |
| content | String | 建议内容 |

---

### 3.2 提交建议接口
**功能描述**：用户提交建议或反馈

**业务规则**：
- 用户必须登录才能使用
- 标签和内容不能为空
- 内容长度建议不超过500字
- 提交成功后不返回具体内容

**请求信息**：
```
POST /api/suggestion
Content-Type: application/json
Authorization: Bearer {token}
```

**请求参数**：
```json
{
  "tag": 1,
  "content": "希望增加一个商品收藏功能，方便收藏感兴趣的商品"
}
```

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| tag | Integer | 是 | 建议标签（1功能建议/2Bug反馈/3体验优化/4其他） | 1 |
| content | String | 是 | 建议内容 | 希望增加一个商品收藏功能 |

**成功响应**：
```json
{
  "code": 0,
  "msg": "提交成功，感谢您的反馈"
}
```

**异常响应**：
```json
// 参数校验失败
{
  "code": 400,
  "msg": "标签和内容不能为空"
}

// 未登录
{
  "code": 401,
  "msg": "请先登录"
}
```

**涉及表**：
- INSERT: biz_suggestion

---

## 4. 后台管理接口

### 4.1 建议CRUD接口
**功能描述**：建议的增删改查基础接口（框架自动生成）

**请求信息**：
```
POST   /biz/suggestion         # 新增建议
PUT    /biz/suggestion         # 修改建议
DELETE /biz/suggestion/{id}    # 删除建议
GET    /biz/suggestion/{id}    # 查询建议详情
GET    /biz/suggestion/page    # 分页查询建议列表
```

**权限要求**：需要管理员权限

**查询参数（分页查询）**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| current | Integer | 是 | 当前页码 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| tag | Integer | 否 | 建议标签 | 1 |
| createUserId | Long | 否 | 提交用户ID | 10 |

---

# 模块六：活动中心

## 1. 接口概述
活动中心模块包含企业内部活动发布、查询和审批功能，员工可以查看已审批通过的活动，管理员可以审批活动申请。

## 2. 数据模型

### 2.1 活动表结构 (biz_activity)
```sql
CREATE TABLE `biz_activity` (
  `id` bigint NOT NULL COMMENT 'id',
  `title` varchar(64) NOT NULL COMMENT '活动标题',
  `image` varchar(128) NOT NULL COMMENT '首图',
  `content` text NOT NULL COMMENT '活动内容',
  `type` tinyint(1) NOT NULL COMMENT '活动类型（1：普通活动；2：分享会）',
  `start_time` datetime NOT NULL COMMENT '活动开始时间',
  `end_time` datetime NOT NULL COMMENT '活动结束时间',
  `activity_people_nums` int NOT NULL COMMENT '活动人数（0代表不限制）',
  `attachment` varchar(500) DEFAULT NULL COMMENT '附件URL（支持文档、PPT、PDF等）',
  `status` tinyint NOT NULL DEFAULT '1' COMMENT '审批状态（1：待审核；2：审核成功；3：审核失败）',
  `is_top` bit(1) NOT NULL DEFAULT b'0' COMMENT '是否置顶',
  `audit_remark` varchar(255) DEFAULT NULL COMMENT '审核备注',
  `audit_time` datetime DEFAULT NULL COMMENT '审核时间',
  `audit_user` bigint DEFAULT NULL COMMENT '审核人ID',
  `create_time` datetime NOT NULL COMMENT '创建时间',
  `create_user` bigint NOT NULL COMMENT '创建人',
  `update_time` datetime DEFAULT NULL COMMENT '更新时间',
  `update_user` bigint DEFAULT NULL COMMENT '更新人',
  `deleted` bigint DEFAULT '0' COMMENT '是否已删除（0：否；id：是）',
  PRIMARY KEY (`id`),
  KEY `idx_type_index` (`type`),
  KEY `index_status` (`status`),
  KEY `index_start_time` (`start_time`),
  KEY `index_end_time` (`end_time`)
) ENGINE=InnoDB  COMMENT='活动表'
```

### 2.2 活动参与用户表 (biz_activity_member)
```sql
CREATE TABLE `biz_activity_member` (
  `id` bigint NOT NULL COMMENT 'id',
  `activity_id` bigint NOT NULL COMMENT '活动id',
  `create_time` datetime NOT NULL COMMENT '创建时间',
  `create_user` bigint NOT NULL COMMENT '创建人',
  `deleted` bigint DEFAULT '0' COMMENT '是否已删除（0：否；id：是）',
  `user_id` bigint NOT NULL COMMENT '参加人员id',
  `type` tinyint NOT NULL COMMENT '参加成员类型（1：演讲人；2：必参加人；3：主动参加）',
  `status` tinyint NOT NULL COMMENT '状态（1：已报名；2：已取消）',
  PRIMARY KEY (`id`),
  KEY `index_activity_id` (`activity_id`),
  KEY `index_user_id` (`user_id`)
) ENGINE=InnoDB  COMMENT='活动参与用户表'
```

### 2.3 参加成员类型字典 (type)
| 值 | 说明 |
|----|------|
| 1 | 演讲人 |
| 2 | 必参加人 |
| 3 | 主动参加 |

### 2.4 参与状态字典 (status)
| 值 | 说明 |
|----|------|
| 1 | 已报名 |
| 2 | 已取消 |

### 2.2 活动类型字典 (type)
| 值 | 说明 |
|----|------|
| 1 | 普通活动 |
| 2 | 分享会 |

### 2.3 审批状态字典 (status)
| 值 | 说明 | 可执行操作 | 客户端可见 |
|----|------|-----------|-----------|
| 1 | 待审核 | 管理员审批 | 不可见 |
| 2 | 审核成功 | 无 | 可见 |
| 3 | 审核失败 | 无 | 不可见 |

---

## 3. 客户端接口

### 3.1 活动分页查询接口
**功能描述**：分页查询已审批通过的活动

**业务规则**：
- 只查询状态为"审核成功"(2)的活动
- 待审核(1)和审核失败(3)的活动客户端不可见
- 置顶活动优先显示（isTop=1的活动排在前面）
- 支持排序（活动开始时间升序/降序、活动结束时间升序/降序、创建时间升序/降序）
- 默认按活动开始时间倒序排列（最近的在前面）

**请求信息**：
```
GET /api/activity
Authorization: Bearer {token}
```

**查询参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| page | Integer | 是 | 当前页码，从1开始 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| sortField | String | 否 | 排序字段（startTime活动开始时间/endTime活动结束时间/createTime创建时间） | startTime |
| sortOrder | String | 否 | 排序方式（asc升序/desc降序） | desc |

**请求示例**：
```
GET /api/activity?page=1&size=10
```

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "records": [
      {
        "id": 1,
        "title": "技术分享会：Spring Cloud微服务架构实践",
        "image": "https://example.com/images/activity1.jpg",
        "startTime": "2026-05-15 14:00:00",
        "endTime": "2026-05-15 17:00:00",
        "createTime": "2026-04-29 10:00:00",
        "createUser": "张三"
      },
      {
        "id": 2,
        "title": "团队建设活动：户外拓展训练",
        "image": "https://example.com/images/activity2.jpg",
        "startTime": "2026-05-20 09:00:00",
        "endTime": "2026-05-20 18:00:00",
        "createTime": "2026-04-28 15:30:00",
        "createUser": "李四"
      }
    ],
    "total": 20,
    "page": 1,
    "size": 10
  }
}
```

**响应字段说明**：
| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | Long | 活动ID |
| title | String | 活动标题 |
| image | String | 活动首图URL |
| startTime | String | 活动开始时间（格式：yyyy-MM-dd HH:mm:ss） |
| endTime | String | 活动结束时间（格式：yyyy-MM-dd HH:mm:ss） |
| createTime | String | 创建时间（格式：yyyy-MM-dd HH:mm:ss） |
| createUser | String | 创建人姓名 |

---

### 3.2 活动详情查询接口
**功能描述**：查询单个活动的详细信息

**业务规则**：
- 只能查询状态为"审核成功"(2)的活动
- 返回活动的完整信息和参与用户列表

**请求信息**：
```
GET /api/activity/{id}
Authorization: Bearer {token}
```

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| id | Long | 是 | 活动ID | 1 |

**请求示例**：
```
GET /api/activity/1
```

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "id": 1,
    "title": "技术分享会：Spring Cloud微服务架构实践",
    "image": "https://example.com/images/activity1.jpg",
    "content": "本次分享会将介绍Spring Cloud微服务架构的最佳实践，包括服务注册发现、配置中心、网关路由等核心组件的使用...",
    "startTime": "2026-05-15 14:00:00",
    "endTime": "2026-05-15 17:00:00",
    "createTime": "2026-04-29 10:00:00",
    "createUser": "张三",
    "type": 2,
    "typeText": "分享会",
    "eventPeopleNums": 50,
    "attachment": "https://example.com/files/spring-cloud-presentation.pdf",
    "members": [
      {
        "userId": 10,
        "userName": "李四",
        "type": 1,
        "typeText": "演讲人",
        "status": 1,
        "statusText": "已报名"
      },
      {
        "userId": 20,
        "userName": "王五",
        "type": 2,
        "typeText": "必参加人",
        "status": 1,
        "statusText": "已报名"
      }
    ]
  }
}
```

**响应字段说明**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | Long | 活动ID |
| title | String | 活动标题 |
| image | String | 活动首图URL |
| content | String | 活动内容 |
| startTime | String | 活动开始时间（格式：yyyy-MM-dd HH:mm:ss） |
| endTime | String | 活动结束时间（格式：yyyy-MM-dd HH:mm:ss） |
| createTime | String | 创建时间（格式：yyyy-MM-dd HH:mm:ss） |
| createUser | String | 创建人姓名 |
| type | Integer | 活动类型 |
| typeText | String | 活动类型文本描述 |
| eventPeopleNums | Integer | 活动人数（0表示不限制） |
| attachment | String | 活动附件URL（可为空） |
| members | Array<Object> | 参加活动的成员列表 |
| members.userId | Long | 用户ID |
| members.userName | String | 用户姓名 |
| members.type | Integer | 成员类型（1演讲人/2必参加人/3主动参加） |
| members.typeText | String | 成员类型文本描述 |
| members.status | Integer | 状态（1已报名/2已取消） |
| members.statusText | String | 状态文本描述 |

**异常响应**：
```json
// 活动不存在
{
  "code": 4012,
  "msg": "活动不存在或已被删除"
}

// 活动未审核通过
{
  "code": 4013,
  "msg": "该活动尚未审核通过"
}
```

---

### 3.3 创建活动接口
**功能描述**：用户创建活动申请

**业务规则**：
- 用户必须登录才能使用
- 创建的活动默认状态为"待审核"(1)，需要管理员审批后才能在前端显示
- 标题、内容、图片、参加人数为必填项
- 参加人员ID列表为选填项

**请求信息**：
```
POST /api/activity
Content-Type: application/json
Authorization: Bearer {token}
```

**请求参数**：
```json
{
  "title": "技术分享会：Spring Cloud微服务架构实践",
  "content": "本次分享会将介绍Spring Cloud微服务架构的最佳实践，包括服务注册发现、配置中心、网关路由等核心组件的使用...",
  "image": "https://example.com/images/activity1.jpg",
  "eventPeopleNums": 50,
  "attachment": "https://example.com/files/spring-cloud-presentation.pdf",
  "members": [
    {
      "userId": 10,
      "type": 1
    },
    {
      "userId": 20,
      "type": 2
    },
    {
      "userId": 30,
      "type": 3
    }
  ]
}
```

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| title | String | 是 | 活动标题 | 技术分享会 |
| content | String | 是 | 活动内容 | 分享会的主要内容介绍 |
| image | String | 是 | 活动首图URL | https://example.com/images/xxx.jpg |
| eventPeopleNums | Integer | 是 | 活动人数（0表示不限制） | 50 |
| attachment | String | 否 | 活动附件URL（支持文档、PPT、PDF等） | https://example.com/files/xxx.pdf |
| members | Array<Object> | 否 | 参加活动的成员列表 | [{"userId":10,"type":1}] |
| members.userId | Long | 是 | 用户ID | 10 |
| members.type | Integer | 是 | 成员类型（1演讲人/2必参加人/3主动参加） | 1 |

**成功响应**：
```json
{
  "code": 0,
  "msg": "活动创建成功，等待管理员审核"
}
```

**异常响应**：
```json
// 参数校验失败
{
  "code": 400,
  "msg": "标题、内容、图片、参加人数不能为空"
}

// 未登录
{
  "code": 401,
  "msg": "请先登录"
}
```

**涉及表**：
- INSERT: biz_activity
- INSERT: biz_activity_member（如果有members）

---

### 3.4 报名活动接口
**功能描述**：用户报名参加活动

**业务规则**：
- 用户必须登录才能使用
- 只能报名状态为"审核成功"(2)的活动
- 校验用户是否已经报名过该活动，避免重复报名
- 校验活动人数是否已满（eventPeopleNums > 0时需要校验）
- 报名成功后状态默认为"已报名"(1)，成员类型默认为"主动参加"(3)

**请求信息**：
```
POST /api/activity/participate/{activityId}
Authorization: Bearer {token}
```

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| activityId | Long | 是 | 活动ID | 1 |

**请求示例**：
```
POST /api/activity/participate/1
```

**成功响应**：
```json
{
  "code": 0,
  "msg": "报名成功"
}
```

**异常响应**：
```json
// 活动不存在
{
  "code": 4012,
  "msg": "活动不存在或已被删除"
}

// 活动未审核通过
{
  "code": 4013,
  "msg": "该活动尚未审核通过，无法报名"
}

// 已经报名过
{
  "code": 4014,
  "msg": "您已经报名过该活动，请勿重复报名"
}

// 活动人数已满
{
  "code": 4015,
  "msg": "抱歉，该活动报名人数已满"
}

// 报名已取消
{
  "code": 4016,
  "msg": "您之前已取消报名，如需重新报名请联系管理员"
}
```

**涉及表**：
- INSERT: biz_activity_member

---

### 3.5 取消报名接口
**功能描述**：用户取消已报名的活动

**业务规则**：
- 用户必须登录才能使用
- 只能取消自己已报名的活动
- 将参与状态从"已报名"(1)更新为"已取消"(2)
- 活动已经开始或已结束时不允许取消

**请求信息**：

```
PUT /api/activity/cancel/{activityId}
Authorization: Bearer {token}
```

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| activityId | Long | 是 | 活动ID | 1 |

**请求示例**：
```
PUT /api/activity/cancel/1
```

**成功响应**：
```json
{
  "code": 0,
  "msg": "取消报名成功"
}
```

**异常响应**：
```json
// 活动不存在
{
  "code": 4012,
  "msg": "活动不存在或已被删除"
}

// 未报名该活动
{
  "code": 4017,
  "msg": "您未报名该活动，无法取消"
}

// 已经取消过了
{
  "code": 4018,
  "msg": "您已经取消过该活动的报名"
}

// 活动已开始或已结束
{
  "code": 4019,
  "msg": "活动已开始或已结束，无法取消报名"
}
```

**涉及表**：
- UPDATE: biz_activity_member（更新status为2）

---

## 4. 后台管理接口

### 4.1 活动CRUD接口
**功能描述**：活动的增删改查基础接口（框架自动生成）

**请求信息**：
```
POST   /biz/activity         # 新增活动
PUT    /biz/activity         # 修改活动
DELETE /biz/activity/{id}    # 删除活动
GET    /biz/activity/{id}    # 查询活动详情
GET    /biz/activity/page    # 分页查询活动列表
```

**权限要求**：需要管理员权限

---

#### 4.1.1 新增活动接口
**功能描述**：管理员创建新活动

**业务规则**：
- 管理员创建活动时可指定演讲人、必参加人等成员
- 创建的活动默认状态为"待审核"(1)，需要管理员审批后才能在前端显示
- 可以设置活动人数限制（0表示不限制）
- 支持设置活动为置顶状态

**请求信息**：
```
POST /biz/activity
Content-Type: application/json
Authorization: Bearer {adminToken}
```

**请求参数**：
```json
{
  "title": "技术分享会：Spring Cloud微服务架构实践",
  "content": "本次分享会将介绍Spring Cloud微服务架构的最佳实践，包括服务注册发现、配置中心、网关路由等核心组件的使用...",
  "image": "https://example.com/images/activity1.jpg",
  "type": 2,
  "startTime": "2026-05-15 14:00:00",
  "endTime": "2026-05-15 17:00:00",
  "eventPeopleNums": 50,
  "isTop": false,
  "attachment": "https://example.com/files/spring-cloud-presentation.pdf",
  "members": [
    {
      "userId": 10,
      "type": 1
    },
    {
      "userId": 20,
      "type": 2
    },
    {
      "userId": 30,
      "type": 3
    }
  ]
}
```

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| title | String | 是 | 活动标题 | 技术分享会 |
| content | String | 是 | 活动内容 | 分享会的主要内容介绍 |
| image | String | 是 | 活动首图URL | https://example.com/images/xxx.jpg |
| type | Integer | 是 | 活动类型（1普通活动/2分享会） | 2 |
| startTime | String | 是 | 活动开始时间（格式：yyyy-MM-dd HH:mm:ss） | 2026-05-15 14:00:00 |
| endTime | String | 是 | 活动结束时间（格式：yyyy-MM-dd HH:mm:ss） | 2026-05-15 17:00:00 |
| eventPeopleNums | Integer | 是 | 活动人数（0表示不限制） | 50 |
| isTop | Boolean | 否 | 是否置顶（默认false） | false |
| attachment | String | 否 | 活动附件URL（支持文档、PPT、PDF等） | https://example.com/files/xxx.pdf |
| members | Array<Object> | 否 | 参加活动的成员列表 | [{"userId":10,"type":1}] |
| members.userId | Long | 是 | 用户ID | 10 |
| members.type | Integer | 是 | 成员类型（1演讲人/2必参加人/3主动参加） | 1 |

**成功响应**：

```json
{
  "code": 0,
  "msg": "活动创建成功"
}
```

**异常响应**：
```json
// 参数校验失败
{
  "code": 400,
  "msg": "标题、内容、图片、活动类型、开始时间、结束时间、参加人数不能为空"
}

// 未登录或权限不足
{
  "code": 401,
  "msg": "请先登录或权限不足"
}
```

**涉及表**：
- INSERT: biz_activity
- INSERT: biz_activity_member（如果有members）

---

### 4.2 活动参与成员CRUD接口
**功能描述**：管理活动参与成员的基础接口（框架自动生成）

**请求信息**：
```
POST   /biz/activityMember         # 新增参与成员（用户报名）
PUT    /biz/activityMember         # 修改参与成员信息
DELETE /biz/activityMember/{id}    # 删除参与成员（取消报名）
GET    /biz/activityMember/{id}    # 查询参与成员详情
GET    /biz/activityMember/page    # 分页查询参与成员列表
```

**权限要求**：
- 后台接口：需要管理员权限
- 用户报名：需要用户登录

**查询参数（分页查询）**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| current | Integer | 是 | 当前页码 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| activityId | Long | 否 | 活动ID | 1 |
| userId | Long | 否 | 用户ID | 10 |
| type | Integer | 否 | 成员类型（1演讲人/2必参加人/3主动参加） | 1 |
| status | Integer | 否 | 状态（1已报名/2已取消） | 1 |

**查询参数（分页查询）**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| current | Integer | 是 | 当前页码 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| title | String | 否 | 活动标题（模糊查询） | 技术 |
| type | Integer | 否 | 活动类型 | 2 |
| status | Integer | 否 | 审批状态（1待审核/2审核成功/3审核失败） | 1 |

### 4.2 活动审批接口
**功能描述**：管理员审批活动申请

**业务规则**：
- 管理员审批待审核状态(1)的活动
- 审批成功(status=2)后，客户端用户可以看到该活动
- 审批失败(status=0)后，客户端用户无法看到该活动
- 记录审核人和审核时间

**请求信息**：
```
POST /biz/activity/approval
Content-Type: application/json
Authorization: Bearer {adminToken}
```

**请求参数**：
```json
{
  "id": 1,
  "status": 2,
  "auditRemark": "活动内容充实，同意举办"
}
```

| 参数名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| id | Long | 是 | 活动ID |
| status | Integer | 是 | 审批状态（2：审核成功；3：审核失败） |
| auditRemark | String | 否 | 审核备注 |

**成功响应**：
```json
{
  "code": 0,
  "msg": "审批成功"
}
```

**异常响应**：
```json
// 活动不存在
{
  "code": 4010,
  "msg": "活动不存在"
}

// 状态参数错误
{
  "code": 400,
  "msg": "审批状态参数错误，只能为0或2"
}
```

---

# 模块七：部门介绍

## 1. 接口概述
部门介绍模块包含企业部门信息展示功能，员工可以查看所有部门的基本信息。

## 2. 数据模型

### 2.1 部门表结构 (sys_dept)
```sql
CREATE TABLE `sys_dept` (
  `id` bigint NOT NULL COMMENT '部门ID',
  `parent_id` bigint DEFAULT '0' COMMENT '父部门ID',
  `name` varchar(64) NOT NULL COMMENT '部门名称',
  `image` varchar(255) DEFAULT NULL COMMENT '部门图片',
  `description` varchar(500) DEFAULT NULL COMMENT '部门描述',
  `sort` int DEFAULT '0' COMMENT '排序',
  `status` tinyint NOT NULL DEFAULT '1' COMMENT '状态（0：禁用；1：启用）',
  `create_time` datetime NOT NULL COMMENT '创建时间',
  `create_user` bigint NOT NULL COMMENT '创建人',
  `update_time` datetime DEFAULT NULL COMMENT '更新时间',
  `update_user` bigint DEFAULT NULL COMMENT '更新人',
  `deleted` bigint DEFAULT '0' COMMENT '是否已删除（0：否；id：是）',
  PRIMARY KEY (`id`),
  KEY `index_parent_id` (`parent_id`),
  KEY `index_status` (`status`)
) ENGINE=InnoDB DEFAULT COMMENT='部门表'
```

---

## 3. 客户端接口

### 3.1 查询全部部门接口
**功能描述**：查询所有启用的部门信息

**业务规则**：
- 只查询状态为"启用"(1)的部门
- 按排序字段升序排列
- 不需要登录即可访问

**请求信息**：
```
GET /api/dept
```

**请求参数**：无

**请求示例**：
```
GET /api/dept
```

**成功响应**：
```json
{
  "code": 0,
  "data": [
    {
      "id": 1,
      "name": "技术部",
      "image": "https://example.com/images/tech-dept.jpg",
      "description": "负责公司核心技术研发、系统架构设计和技术攻关，是公司的技术核心力量。"
    },
    {
      "id": 2,
      "name": "市场部",
      "image": "https://example.com/images/market-dept.jpg",
      "description": "负责市场调研、品牌推广、营销策划和客户关系维护，推动公司业务发展。"
    },
    {
      "id": 3,
      "name": "人力资源部",
      "image": "https://example.com/images/hr-dept.jpg",
      "description": "负责人才招聘、员工培训、绩效考核和企业文化建设，为公司发展提供人才保障。"
    },
    {
      "id": 4,
      "name": "财务部",
      "image": "https://example.com/images/finance-dept.jpg",
      "description": "负责公司财务管理、预算控制、资金运作和财务报表，确保公司财务健康稳定。"
    }
  ]
}
```

**响应字段说明**：
| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | Long | 部门ID |
| name | String | 部门名称 |
| image | String | 部门图片URL |
| description | String | 部门描述 |

**异常响应**：
```json
// 查询失败
{
  "code": 500,
  "msg": "查询部门信息失败"
}
```

---

### 3.2 部门详情查询接口
**功能描述**：查询单个部门的详细信息和部门成员列表

**业务规则**：

- 只能查询状态为"启用"(1)的部门
- 返回部门的基本信息和成员列表
- 只返回状态为"启用"(1)的部门成员
- 部门成员按职位排序或创建时间排序

**请求信息**：
```
GET /api/dept/{deptId}
```

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| deptId | Long | 是 | 部门ID | 1 |

**请求示例**：
```
GET /api/dept/1
```

**成功响应**：

```json
{
  "code": 0,
  "data": {
    "id": 1,
    "name": "技术部",
    "description": "负责公司核心技术研发、系统架构设计和技术攻关，是公司的技术核心力量。团队成员来自国内外知名高校，技术实力雄厚，致力于打造行业领先的技术产品和解决方案。",
    "members": [
      {
        "userId": 10,
        "nickname": "张三",
        "avatar": "https://example.com/avatars/user10.jpg",
        "phone": "13800138001",
        "position": "技术总监",
        "signature": "追求卓越，技术驱动创新"
      },
      {
        "userId": 20,
        "nickname": "李四",
        "avatar": "https://example.com/avatars/user20.jpg",
        "phone": "13800138002",
        "position": "高级Java工程师",
        "signature": "代码改变世界"
      },
      {
        "userId": 30,
        "nickname": "王五",
        "avatar": "https://example.com/avatars/user30.jpg",
        "phone": "13800138003",
        "position": "前端工程师",
        "signature": "用技术创造更好的用户体验"
      }
    ]
  }
}
```

**响应字段说明**：
| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | Long | 部门ID |
| name | String | 部门名称 |
| description | String | 部门描述 |
| members | Array<Object> | 部门成员列表 |
| members.userId | Long | 用户ID |
| members.nickname | String | 用户昵称 |
| members.avatar | String | 用户头像URL |
| members.phone | String | 手机号 |
| members.position | String | 职位 |
| members.signature | String | 个性签名 |

**异常响应**：
```json
// 部门不存在
{
  "code": 4020,
  "msg": "部门不存在或已被删除"
}

// 部门已禁用
{
  "code": 4021,
  "msg": "该部门已被禁用"
}
```

---

# 模块八：消息管理

## 1. 接口概述
消息管理模块包含系统消息和安全消息的查询功能，管理员可以发布通知，员工可以查看各类消息通知。

## 2. 数据模型

### 2.1 消息表结构 (sys_message)
```sql
CREATE TABLE `sys_message` (
  `id` bigint NOT NULL COMMENT 'ID',
  `title` varchar(128) NOT NULL COMMENT '标题',
  `content` text NOT NULL COMMENT '内容',
  `type` tinyint NOT NULL COMMENT '类型（1：系统消息；2：安全消息）',
  `path` varchar(255) DEFAULT NULL COMMENT '跳转路径',
  `scope` tinyint NOT NULL COMMENT '通知范围（1：所有人；2：指定用户）',
  `users` json DEFAULT NULL COMMENT '通知用户（scope=2时存储用户ID列表）',
  `create_time` datetime NOT NULL COMMENT '创建时间',
  `update_time` datetime DEFAULT NULL COMMENT '修改时间',
  `deleted` bigint DEFAULT '0' COMMENT '是否已删除（0：否；id：是）',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB COMMENT='消息表'
```

### 2.2 消息类型字典 (type)
| 值 | 说明 |
|----|------|
| 1 | 系统消息 |
| 2 | 安全消息 |

### 2.3 通知范围字典 (scope)
| 值 | 说明 |
|----|------|
| 1 | 所有人 |
| 2 | 指定用户 |

---

## 3. 客户端接口

### 3.1 消息分页查询接口
**功能描述**：分页查询当前登录用户的消息列表

**业务规则**：
- 用户必须登录才能使用
- 查询当前用户可见的消息：
  - 通知范围为"所有人"(scope=1)的消息
  - 通知范围为"指定用户"(scope=2)且 users 字段包含当前用户ID的消息
- 支持按消息类型筛选（1系统消息/2安全消息）
- 支持排序（创建时间升序/降序、消息类型升序/降序）
- 默认按创建时间倒序排列（最新的在前面）

**请求信息**：
```
GET /api/message
Authorization: Bearer {token}
```

**查询参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| page | Integer | 是 | 当前页码，从1开始 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| type | Integer | 否 | 消息类型（1系统消息/2安全消息），不传则查询所有类型 | 1 |
| sortField | String | 否 | 排序字段（createTime创建时间/type消息类型） | createTime |
| sortOrder | String | 否 | 排序方式（asc升序/desc降序） | desc |

**请求示例**：
```
# 查询所有类型的消息
GET /api/message?page=1&size=10

# 查询系统消息
GET /api/message?page=1&size=10&type=1
```

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "records": [
      {
        "id": 1,
        "type": 1,
        "typeText": "系统消息",
        "title": "系统维护通知",
        "content": "尊敬的各位同事：系统将于2026年5月1日00:00至06:00进行升级维护，届时部分功能将暂停使用，请提前做好相关准备。",
        "path": null,
        "createTime": "2026-04-29 10:00:00"
      },
      {
        "id": 2,
        "type": 1,
        "typeText": "系统消息",
        "title": "新功能上线通知",
        "content": "员工内部系统新增心愿管理功能，欢迎各位同事体验并提出宝贵建议。",
        "path": "/pages/wish/list",
        "createTime": "2026-04-28 15:30:00"
      },
      {
        "id": 3,
        "type": 2,
        "typeText": "安全消息",
        "title": "密码安全提醒",
        "content": "请定期更换您的登录密码，确保账户安全。",
        "path": null,
        "createTime": "2026-04-27 09:20:00"
      }
    ],
    "total": 50,
    "page": 1,
    "size": 10
  }
}
```

**响应字段说明**：
| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | Long | 消息ID |
| type | Integer | 消息类型（1系统消息/2安全消息） |
| typeText | String | 消息类型文本描述 |
| title | String | 消息标题 |
| content | String | 消息内容 |
| path | String | 跳转路径（可为空） |
| createTime | String | 创建时间（格式：yyyy-MM-dd HH:mm:ss） |

**异常响应**：
```json
// 未登录
{
  "code": 401,
  "msg": "请先登录"
}
```

---

# 模块九：用户个人信息

## 1. 接口概述
用户个人信息模块包含员工个人信息的查询功能，员工可以查看自己的详细资料。

## 2. 数据模型

### 2.1 用户表结构 (sys_user) - 个人信息相关字段
```sql
-- 用户表中个人信息相关字段
`phone` varchar(20) DEFAULT NULL COMMENT '联系电话',
`signature` varchar(255) DEFAULT NULL COMMENT '个性签名',
`dept_id` bigint DEFAULT NULL COMMENT '所属部门ID',
`position` varchar(64) DEFAULT NULL COMMENT '当前职位',
`hire_date` date DEFAULT NULL COMMENT '入职时间',
`birthday` date DEFAULT NULL COMMENT '生日',
`avatar` varchar(255) DEFAULT NULL COMMENT '头像URL',
`nickname` varchar(64) DEFAULT NULL COMMENT '昵称'
```

---

## 3. 客户端接口

### 3.1 用户个人信息查询接口
**功能描述**：查询当前登录用户的个人信息

**业务规则**：
- 用户必须登录才能使用
- 返回当前登录用户的完整个人信息
- 关联查询部门名称

**请求信息**：
```
GET /api/user/info
Authorization: Bearer {token}
```

**请求参数**：无

**请求示例**：
```
GET /api/user/info
```

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "userId": 10,
    "nickname": "张三",
    "avatar": "https://example.com/avatars/user10.jpg",
    "phone": "13800138001",
    "signature": "追求卓越，技术驱动创新",
    "deptId": 1,
    "deptName": "技术部",
    "position": "高级Java工程师",
    "hireDate": "2023-06-15",
    "birthday": "1995-08-20"
  }
}
```

**响应字段说明**：
| 字段名 | 类型 | 说明 |
|-------|------|------|
| userId | Long | 用户ID |
| nickname | String | 用户昵称 |
| avatar | String | 头像URL |
| phone | String | 联系电话 |
| signature | String | 个性签名 |
| deptId | Long | 所属部门ID |
| deptName | String | 所属部门名称 |
| position | String | 当前职位 |
| hireDate | String | 入职时间（格式：yyyy-MM-dd） |
| birthday | String | 生日（格式：yyyy-MM-dd） |

**异常响应**：
```json
// 未登录
{
  "code": 401,
  "msg": "请先登录"
}

// 用户不存在
{
  "code": 4022,
  "msg": "用户不存在"
}
```

---

### 3.2 用户统计信息接口
**功能描述**：查询当前登录用户的统计数据

**业务规则**：
- 用户必须登录才能使用
- 统计当前用户报名的活动数量和活动ID列表
- 统计当前用户参加的问卷数量和问卷ID列表
- 只统计状态为"已报名"(1)的活动

**请求信息**：
```
GET /api/stat
Authorization: Bearer {token}
```

**请求参数**：无

**请求示例**：
```
GET /api/stat
```

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "activityCount": 3,
    "activityIds": [1, 5, 8],
    "questionnaireCount": 5,
    "questionnaireIds": [2, 4, 6, 9, 12]
  }
}
```

**响应字段说明**：
| 字段名 | 类型 | 说明 |
|-------|------|------|
| activityCount | Integer | 报名活动的数量（状态为已报名） |
| activityIds | Array<Long> | 报名活动的ID集合 |
| questionnaireCount | Integer | 参加问卷的数量 |
| questionnaireIds | Array<Long> | 参加问卷的ID集合 |

**异常响应**：
```json
// 未登录
{
  "code": 401,
  "msg": "请先登录"
}
```

---

### 3.3 修改密码接口
**功能描述**：用户修改密码

**业务规则**：
- 用户必须登录才能使用
- 需要提供旧密码进行验证
- 新密码必须大于8位，且包含大小写字母和数字
- 新密码不能与旧密码相同
- 密码修改后立即生效

**请求信息**：
```
POST /api/password/update
Authorization: Bearer {token}
Content-Type: application/json
```

**请求参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| oldPassword | String | 是 | 旧密码 | "OldPassword123" |
| newPassword | String | 是 | 新密码（大于8位，包含大小写字母和数字） | "NewPassword123" |

**请求示例**：
```json
POST /api/password/update
{
  "oldPassword": "OldPassword123",
  "newPassword": "NewPassword123"
}
```

**成功响应**：
```json
{
  "code": 0,
  "msg": "密码修改成功"
}
```

**异常响应**：
```json
// 未登录
{
  "code": 401,
  "msg": "请先登录"
}

// 旧密码不正确
{
  "code": 400,
  "msg": "旧密码不正确"
}

// 新密码不符合要求
{
  "code": 400,
  "msg": "密码必须大于8位，且包含大小写字母和数字"
}

// 新旧密码相同
{
  "code": 400,
  "msg": "新密码不能与旧密码相同"
}
```

---

# 模块十：心愿管理

## 1. 接口概述
心愿管理模块包含员工心愿提交和查询功能，员工可以提交心愿建议，管理员可以审核后将心愿转换为商品。

## 2. 数据模型

### 2.1 心愿表结构 (biz_wish)
```sql
create table biz_wish
(
    id    		bigint               not null comment 'id'
        primary key,
    name        varchar(50)          not null comment '商品名称',
    product_id  bigint               null comment '转成商品的id',
    is_product  bit     default b'0' not null comment '是否转成商品',
    create_time datetime             not null comment '创建时间',
    create_user bigint               not null comment '创建人',
    deleted     bigint  default 0    null comment '是否已删除（0：否；id：是）',
    parents_id  bigint  default 0    not null comment '父id 如果为0 代表是父',
    status      tinyint default 1    not null comment '许愿状态 1-心愿中 2-许愿成功 3-许愿失败  4-许愿取消',
    fail_reason varchar(255)         null comment '失败原因'
); ENGINE=InnoDB COMMENT='心愿表'
```

### 2.2 心愿状态字典 (status)
| 值 | 说明 | 客户端可见 |
|----|------|-----------|
| 1 | 心愿中 | 可见 |
| 2 | 许愿成功 | 可见 |
| 3 | 许愿失败 | 可见 |
| 4 | 许愿取消 | 可见 |



---

## 3. 客户端接口

### 3.1 新增心愿接口
**功能描述**：用户提交心愿建议

**业务规则**：
- 用户必须登录才能使用
- 提交的心愿默认状态为"待转换"(1)
- 等待管理员审核决定是否转换为商品

**请求信息**：
```
POST /api/wish/add
Content-Type: application/json
Authorization: Bearer {token}
```

**请求参数**：
```json
{
  "wishId": 5,
  "name": "iPad Pro",
  "reason": "希望上架iPad Pro，用于移动办公和设计工作"
}
```

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| wishId | Long | 否 | 已有心愿ID（选择已有心愿时填写）  为父id | 5 |
| name | String | 是 | 商品名称 | iPad Pro |
| reason | String | 否 | 推荐理由 | 用于移动办公 |

**成功响应**：
```json
{
  "code": 0,
  "msg": "心愿提交成功"
}
```

**异常响应**：
```json
// 参数校验失败
{
  "code": 400,
  "msg": "心愿标题不能为空"
}

// 未登录
{
  "code": 401,
  "msg": "请先登录"
}
```

**涉及表**：

- INSERT: biz_wish

---

### 3.2 我的心愿分页查询接口
**功能描述**：分页查询当前登录用户提交的心愿记录

**业务规则**：
- 用户必须登录才能使用
- 只查询当前登录用户提交的心愿
- 支持排序（创建时间升序/降序、状态升序/降序）
- 默认按创建时间倒序排列（最新的在前面）

**请求信息**：
```
GET /api/wish
Authorization: Bearer {token}
```

**查询参数**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| current | Integer | 是 | 当前页码，从1开始 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| sortField | String | 否 | 排序字段（createTime创建时间/status状态） | createTime |
| sortOrder | String | 否 | 排序方式（asc升序/desc降序） | desc |

**请求示例**：
```
GET /api/wish?page=1&size=10
```

**成功响应**：
```json
{
  "code": 0,
  "data": {
    "records": [
      {
        "id": 1,
        "title": "iPad Pro",
        "createTime": "2026-04-29 10:00:00",
        "status": 1,
        "statusText": "待转换"
      },
      {
        "id": 2,
        "title": "AirPods Pro",
        "createTime": "2026-04-28 15:30:00",
        "status": 2,
        "statusText": "已转换"
      },
      {
        "id": 3,
        "title": "机械键盘",
        "createTime": "2026-04-27 09:20:00",
        "status": 3,
        "statusText": "已拒绝"
      }
    ],
    "total": 20,
    "page": 1,
    "size": 10
  }
}
```

**响应字段说明**：
| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | Long | 心愿ID |
| title | String | 心愿名称 |
| createTime | String | 提交时间（格式：yyyy-MM-dd HH:mm:ss） |
| status | Integer | 审核状态 |
| statusText | String | 审核状态文本描述 |

---

## 4. 后台管理接口

### 4.1 心愿CRUD接口
**功能描述**：心愿的增删改查基础接口（框架自动生成）

**请求信息**：
```
POST   /biz/wish         # 新增心愿
PUT    /biz/wish         # 修改心愿
DELETE /biz/wish/{id}    # 删除心愿
GET    /biz/wish/{id}    # 查询心愿详情
GET    /biz/wish/page    # 分页查询心愿列表
```

**权限要求**：需要管理员权限

**查询参数（分页查询）**：
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| current | Integer | 是 | 当前页码 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| title | String | 否 | 心愿标题（模糊查询） | iPad |
| status | Integer | 否 | 状态 | 1 |
| userId | Long | 否 | 提交用户ID | 10 |

### 4.2 心愿转商品接口
**功能描述**：管理员将心愿转换为商品

**业务规则**：
- 只能转换状态为"待转换"(1)的心愿
- 转换后创建新的商品记录
- 心愿状态更新为"已转换"(2)
- 记录转换时间和操作人

**请求信息**：
```
POST /biz/wish/wishToProduct
Content-Type: application/json
Authorization: Bearer {adminToken}
```

**请求参数**：
```json
{
  "wishId": 1,
  "name": "iPad Pro",
  "points": 5000.00,
  "productTypeId": 2,
  "image": "https://example.com/images/ipad-pro.jpg",
  "description": "苹果iPad Pro，11英寸，256GB，深空灰色",
  "stock": 10
}
```

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| wishId | Long | 是 | 心愿ID | 1 |
| name | String | 是 | 商品名称 | iPad Pro |
| points | BigDecimal | 是 | 商品所需积分 | 5000.00 |
| productTypeId | Long | 是 | 商品类型ID | 2 |
| image | String | 是 | 商品图片URL | https://example.com/xxx.jpg |
| description | String | 是 | 商品描述 | 商品详细描述 |
| stock | Integer | 是 | 月限购数量 | 10 |

**成功响应**：
```json
{
  "code": 0,
  "msg": "心愿转换成功"
}
```

**异常响应**：
```json
// 心愿不存在
{
  "code": 4024,
  "msg": "心愿不存在或已被删除"
}

// 心愿已转换
{
  "code": 4025,
  "msg": "该心愿已转换，不能重复操作"
}

// 心愿状态不允许
{
  "code": 4026,
  "msg": "该心愿状态不允许转换"
}
```

**涉及表**：
- INSERT: biz_product（创建商品）
- UPDATE: biz_wish（更新心愿状态为已转换）

---

# 模块十一：轮播图管理

## 1. 接口概述
轮播图管理模块包含客户端轮播图查询功能，管理员可以配置轮播图，客户端用户可以查看轮播图列表。

## 2. 数据模型

### 2.1 轮播图表结构 (biz_carousel_image)
```sql
CREATE TABLE `biz_carousel_image` (
  `id` bigint NOT NULL COMMENT 'id',
  `url` varchar(128) NOT NULL COMMENT '图片地址',
  `sort` tinyint(1) NOT NULL COMMENT '顺序',
  `title` varchar(255) DEFAULT NULL COMMENT '轮播图标题',
  `jump_path` varchar(255)  DEFAULT NULL COMMENT '跳转路径',
  `create_time` datetime NOT NULL COMMENT '创建时间',
  `create_user` bigint NOT NULL COMMENT '创建人',
  PRIMARY KEY (`id`) USING BTREE
) ENGINE=InnoDB COMMENT='轮播图表'
```

### 2.2 字段说明
| 字段名 | 类型 | 说明 | 示例 |
|-------|------|------|------|
| id | bigint | 轮播图ID | 1 |
| url | varchar(128) | 图片地址 | https://example.com/images/banner1.jpg |
| sort | tinyint(1) | 显示顺序（升序排列） | 1 |
| title | varchar(255) | 轮播图标题 | 新员工入职培训 |
| jump_path | varchar(255) | 跳转路径（可为空） | /pages/activity/detail/1 |
| create_time | datetime | 创建时间 | 2026-04-29 10:00:00 |
| create_user | bigint | 创建人ID | 1 |

---

## 3. 客户端接口

### 3.1 查询轮播图列表接口
**功能描述**：查询所有轮播图列表，按顺序升序排序

**业务规则**：
- 无需登录即可访问
- 返回所有轮播图数据
- 按 sort 字段升序排序（数字小的在前）

**请求信息**：
```
GET /api/carousel
```

**请求参数**：无

**请求示例**：
```
GET /api/carousel
```

**成功响应**：
```json
{
  "code": 0,
  "msg": "操作成功",
  "data": [
    {
      "id": 1,
      "url": "https://example.com/images/banner1.jpg",
      "sort": 1,
      "title": "新员工入职培训",
      "jumpPath": "/pages/activity/detail/1",
    },
    {
      "id": 2,
      "url": "https://example.com/images/banner2.jpg",
      "sort": 2,
      "title": "技术分享会",
      "jumpPath": "/pages/activity/detail/2",
    },
    {
      "id": 3,
      "url": "https://example.com/images/banner3.jpg",
      "sort": 3,
      "title": "团队建设活动",
      "jumpPath": null,
    }
  ]
}
```

**响应字段说明**：
| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | Long | 轮播图ID |
| url | String | 图片地址（完整URL） |
| sort | Integer | 显示顺序（1、2、3...） |
| title | String | 轮播图标题 |
| jumpPath | String | 跳转路径（可为空） |
| createTime | String | 创建时间（格式：yyyy-MM-dd HH:mm:ss） |
| createUser | Long | 创建人ID |

**异常响应**：
```json
// 查询失败
{
  "code": 500,
  "msg": "查询轮播图失败"
}
```

**使用场景**：

- 首页轮播图展示
- 活动推广展示
- 重要通知展示

---

## 4. 后台管理接口

### 4.1 轮播图CRUD接口
**功能描述**：轮播图的增删改查基础接口（框架自动生成）

**请求信息**：
```
POST   /biz/carouselImage         # 新增轮播图
PUT    /biz/carouselImage         # 修改轮播图
DELETE /biz/carouselImage/{id}    # 删除轮播图
GET    /biz/carouselImage/{id}    # 查询轮播图详情
GET    /biz/carouselImage/page    # 分页查询轮播图列表
```

**权限要求**：需要管理员权限

**查询参数（分页查询）**：

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| page | Integer | 是 | 当前页码 | 1 |
| size | Integer | 是 | 每页数量 | 10 |
| title | String | 否 | 轮播图标题（模糊查询） | 培训 |

---

**文档版本**：v1.0
**最后更新**：2026-04-29
**维护人员**：weilai