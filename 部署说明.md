# 应用部署说明

本项目已拆分为两个独立的应用，可以分别部署运行。

## 应用列表

### 1. 管理后台应用
- **模块**: continew-server
- **启动类**: `top.continew.admin.ContiNewAdminApplication`
- **包含功能**:
  - 系统管理（用户、角色、权限、菜单等）
  - 订单管理（管理员审核、发货等）
  - 部门管理
  - 其他后台管理功能

### 2. 员工客户端应用
- **模块**: continew-customer
- **启动类**: `top.continew.admin.customer.CustomerApplication`
- **包含功能**:
  - 员工下单
  - 商品浏览
  - 积分查询
  - 其他员工端功能

## 本地开发运行

### 管理后台
```bash
cd continew-server
mvn spring-boot:run
```
访问地址：http://localhost:8080

### 员工客户端
```bash
cd continew-customer
mvn spring-boot:run
```
访问地址：根据配置文件（默认可能也是 8080，需要在配置文件中修改）

## 打包部署

### 1. 打包管理后台
```bash
cd continew-server
mvn clean package -Dspotless.skip=true
java -jar target/continew-admin.jar
```

### 2. 打包员工客户端
```bash
cd continew-customer
mvn clean package -Dspotless.skip=true
java -jar target/continew-customer.jar
```

## 端口配置

两个应用需要使用不同的端口，请检查配置文件：

### 管理后台端口
- 配置文件：`continew-server/src/main/resources/config/application.yml`
- 默认端口：8080

### 员工客户端端口
- 配置文件：`continew-customer/src/main/resources/config/application.yml`
- 建议端口：8081（需要手动配置）

## 数据库

两个应用可以共用同一个数据库，因为它们操作的是不同的业务表和共享的基础表（通过 hr-common 模块）。

## 注意事项

1. **Bean 冲突已解决**: 
   - `UserMapper` 使用 `@Primary` 注解，hr-common 的为首选
   - `AuthServiceImpl` 和 `OrderServiceImpl` 通过不同的 bean 名称区分

2. **共享实体**: 
   - UserDO、DeptDO、OrderDO 等实体在 hr-common 模块中共享
   - 两个应用可以操作同一套基础数据

3. **独立部署**:
   - 两个应用可以部署在不同的服务器上
   - 可以根据负载独立扩展

## 故障排查

如果遇到启动问题：
1. 检查端口是否冲突
2. 检查数据库连接配置
3. 确保依赖的 hr-common 模块已正确安装
